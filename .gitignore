# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/

# Logs
logs/
*.log
*.jsonl
!backend/info_example.jsonl

# Temp scripts
git_commit.sh

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local

# Agora-2 runtime profiles (keep demo_* committed)
backend/profiles/web_*.json
# Local test-account profiles. Named accounts rather than web_* ids, so they need
# their own line; add new ones here rather than committing them.
backend/profiles/MapTester.json

# Cross-session memory (user_id__scenario_type.jsonl)
backend/memory/

# SQLite user DB
backend/data/

# Participant credential sheets (plaintext). Generated by
# backend/scripts/make_test_users.py; only the hashes in backend/seed_users.json
# are committed. Keep the sheet — a lost password cannot be recovered from a hash.
test_accounts_*.csv

# Node
node_modules/
paper/.vite/
